// enum
// used when you wanna declare an object that has a SET AMOUNT OF THINGS
const enum Directions {
    up = 1,
    down = 1,
    left = 1,
    right = 1
}
const enum TshirtSizes {
    Small = "s",
    Medium = "m",
    Large = "l",

}
const right = Directions.right
const left = Directions.left
const joeSize = TshirtSizes.Small

const enum Ages  {
    young = 1,
    adult,
    old,
}

const oldMan = Ages.old